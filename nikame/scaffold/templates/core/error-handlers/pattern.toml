name = "Global Error Handlers"
category = "core"
tags = ["core", "exceptions", "error-handling"]
description = "Global exception handler hierarchy and standardized error responses."
difficulty = "beginner"
deps = ["pydantic"]
depends_on = ["core/response-models"]
files = [
  { src = "exceptions.py", dest = "core/exceptions.py", merge = "replace" },
  { src = "handlers.py", dest = "core/handlers.py", merge = "replace" },
]
template_vars = []
related = ["middleware/error-boundary", "core/response-models"]
