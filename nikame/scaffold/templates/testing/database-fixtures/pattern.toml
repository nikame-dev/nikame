name = "Database Test Fixtures"
slug = "testing/database-fixtures"
category = "testing"
tags = ["testing", "database", "sqlalchemy", "rollback"]
description = "Atomic DB tests: SQLite in-memory / Docker Postgres, nested transactions for instant rollback"
difficulty = "advanced"
deps = ["pytest", "sqlalchemy", "asyncpg"]
depends_on = ["database/async-session"]

[[files]]
src = "db_conftest.py"
dest = "tests/db_conftest.py"
merge = "replace"

template_vars = ["TEST_DATABASE_URL"]
related = ["testing/async-client"]
