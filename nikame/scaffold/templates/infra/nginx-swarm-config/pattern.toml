name = "NGINX Reverse Proxy Config"
slug = "infra/nginx-swarm-config"
category = "infra"
tags = ["nginx", "infra", "proxy", "docker-swarm", "k8s"]
description = "Optimized NGINX configuration for FastAPI in containerized environments"
difficulty = "intermediate"
deps = []
depends_on = []

[[files]]
src = "nginx.conf"
dest = "infra/nginx.conf"
merge = "replace"

[[files]]
src = "default.conf"
dest = "infra/conf.d/default.conf"
merge = "replace"

template_vars = ["SERVER_NAME"]
related = ["infra/multi-stage-docker"]
