name = "Conditional Routes"
category = "routing"
tags = ["routing", "feature-flags", "conditional"]
description = "Feature-flagged routers that are only mounted if enabled in settings."
difficulty = "beginner"
deps = ["fastapi"]
depends_on = ["routing/app-factory", "core/settings"]
files = [
  { src = "conditional.py", dest = "routers/conditional.py", merge = "replace" },
]
template_vars = []
