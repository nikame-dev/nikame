name = "Redis Rate Limiter"
slug = "auth/rate-limiter"
category = "auth"
tags = ["auth", "security", "rate-limit", "redis", "redis"]
description = "Redis sliding window rate limiter as a dependency, per-user and per-IP"
difficulty = "advanced"
deps = ["redis", "fastapi"]
depends_on = ["core/error-handlers"]

[[files]]
src = "rate_limit.py"
dest = "auth/rate_limit.py"
merge = "replace"

template_vars = ["REDIS_URL"]
related = ["auth/api-key", "performance/redis-cache"]
