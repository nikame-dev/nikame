name = "API Key Auth"
slug = "auth/api-key"
category = "auth"
tags = ["auth", "api-key", "security"]
description = "API key auth via X-API-Key header and ?api_key= query param"
difficulty = "beginner"
deps = ["fastapi", "pydantic"]
depends_on = ["core/error-handlers"]

[[files]]
src = "api_key.py"
dest = "auth/api_key.py"
merge = "replace"

template_vars = []
related = ["auth/jwt", "auth/rate-limiter"]
