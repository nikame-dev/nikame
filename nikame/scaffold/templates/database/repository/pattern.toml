name = "Generic Repository"
slug = "database/repository"
category = "database"
tags = ["database", "sqlalchemy", "repository", "crud"]
description = "Generic async repository base class with typed generics"
difficulty = "intermediate"
deps = ["sqlalchemy", "pydantic"]
depends_on = ["database/async-session"]

[[files]]
src = "repository.py"
dest = "database/repository.py"
merge = "replace"

template_vars = []
related = ["database/unit-of-work"]
