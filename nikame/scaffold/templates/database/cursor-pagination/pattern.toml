name = "Cursor Pagination"
slug = "database/cursor-pagination"
category = "database"
tags = ["database", "sqlalchemy", "pagination", "cursor"]
description = "Cursor-based pagination dep + response builder, works with any SQLAlchemy query"
difficulty = "advanced"
deps = ["sqlalchemy", "pydantic", "fastapi"]
depends_on = []

[[files]]
src = "pagination.py"
dest = "database/cursor_pagination.py"
merge = "replace"

template_vars = []
related = ["database/offset-pagination"]
