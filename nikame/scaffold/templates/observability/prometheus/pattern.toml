name = "Prometheus Metrics"
slug = "observability/prometheus"
category = "observability"
tags = ["observability", "metrics", "prometheus", "monitoring"]
description = "Prometheus instrumentation: request counts, latency histograms, GPU memory tracking"
difficulty = "intermediate"
deps = ["prometheus-fastapi-instrumentator", "prometheus-client"]
depends_on = []

[[files]]
src = "metrics.py"
dest = "observability/metrics.py"
merge = "replace"

template_vars = []
related = ["observability/health-checks"]
