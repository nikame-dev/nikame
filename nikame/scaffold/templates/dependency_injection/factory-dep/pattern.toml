name = "Factory Dependency Pattern"
category = "dependency_injection"
tags = ["di", "depends", "factory", "providers"]
description = "Closure-based factory that returns different implementations based on config."
difficulty = "advanced"
deps = ["fastapi", "pydantic"]
depends_on = ["core/settings"]
files = [
  { src = "factory.py", dest = "dependencies/factory.py", merge = "replace" },
]
template_vars = []
