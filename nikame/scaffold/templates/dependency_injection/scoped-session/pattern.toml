name = "Scoped Session Dependency"
category = "dependency_injection"
tags = ["di", "database", "sqlalchemy", "async"]
description = "Async SQLAlchemy session scoped to request with proper cleanup on exception."
difficulty = "intermediate"
deps = ["fastapi", "sqlalchemy", "asyncpg"]
depends_on = []
files = [
  { src = "scoped.py", dest = "dependencies/scoped_session.py", merge = "replace" },
]
template_vars = []
