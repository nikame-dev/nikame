name = "Dependency Override"
slug = "dependency_injection/dep-override"
category = "dependency_injection"
tags = ["di", "depends", "testing", "fixtures"]
description = "Pattern for test-time dependency overrides with realistic fixtures"
difficulty = "advanced"
deps = ["pytest", "fastapi"]
depends_on = []

[[files]]
src = "override_fixture.py"
dest = "tests/fixtures/override.py"
merge = "append-exports"

template_vars = []
related = ["database/async-session"]
