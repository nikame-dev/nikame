name = "Layered Dependencies"
category = "dependency_injection"
tags = ["di", "depends", "auth", "chains"]
description = "Full dependency chain: get_db → get_token → get_user → require_role."
difficulty = "intermediate"
deps = ["fastapi", "pydantic"]
depends_on = []
files = [
  { src = "layered.py", dest = "dependencies/layered.py", merge = "replace" },
]
template_vars = []
related = ["auth/jwt", "auth/rbac"]
