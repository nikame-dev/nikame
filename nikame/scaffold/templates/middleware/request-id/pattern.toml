name = "Request ID Injection"
slug = "middleware/request-id"
category = "middleware"
tags = ["middleware", "tracing", "observability"]
description = "UUID request ID injection into headers and context"
difficulty = "beginner"
deps = ["fastapi", "uuid"]
depends_on = []

[[files]]
src = "request_id.py"
dest = "middleware/request_id.py"
merge = "replace"

template_vars = []
related = ["middleware/structured-logger"]
