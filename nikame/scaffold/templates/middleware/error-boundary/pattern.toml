name = "Global Error Boundary"
slug = "middleware/error-boundary"
category = "middleware"
tags = ["middleware", "errors", "security"]
description = "Catch-all middleware preventing traceback leaks"
difficulty = "intermediate"
deps = ["fastapi", "structlog"]
depends_on = ["core/error-handlers"]

[[files]]
src = "boundary.py"
dest = "middleware/boundary.py"
merge = "replace"

template_vars = []
related = []
