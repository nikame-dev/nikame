name = "Structured Logger Middleware"
slug = "middleware/structured-logger"
category = "middleware"
tags = ["middleware", "logging", "observability", "structlog"]
description = "Full request/response logging with structlog: method, path, status, duration"
difficulty = "intermediate"
deps = ["fastapi", "structlog"]
depends_on = ["middleware/request-id", "middleware/latency-timer"]

[[files]]
src = "logger.py"
dest = "middleware/logger.py"
merge = "replace"

template_vars = []
related = ["observability/structlog-setup"]
