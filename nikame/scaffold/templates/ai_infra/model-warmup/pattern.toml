name = "Model Warmup"
slug = "ai_infra/model-warmup"
category = "ai_infra"
tags = ["ai", "performance", "warmup", "startup", "latency"]
description = "Warmup pattern: run N dummy inference passes after load to eliminate cold-start latency"
difficulty = "intermediate"
deps = ["fastapi"]
depends_on = ["ai_infra/model-loader"]

[[files]]
src = "warmup.py"
dest = "ai_infra/warmup.py"
merge = "replace"

template_vars = ["MODEL_WARMUP_RUNS"]
related = ["ai_infra/model-loader"]
