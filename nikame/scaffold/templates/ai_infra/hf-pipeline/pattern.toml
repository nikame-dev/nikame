name = "Hugging Face Pipeline"
slug = "ai_infra/hf-pipeline"
category = "ai_infra"
tags = ["ai", "huggingface", "transformers", "pipeline"]
description = "Standard Hugging Face transformers.pipeline loader wrapped for FastAPI"
difficulty = "intermediate"
deps = ["transformers", "torch"]
depends_on = ["ai_infra/model-loader"]

[[files]]
src = "hf_pipeline.py"
dest = "ai_infra/hf_pipeline.py"
merge = "replace"

template_vars = ["PIPELINE_TASK", "HF_MODEL_NAME"]
related = ["ai_infra/hf-transformers-manual"]
