name = "GGUF llama-cpp"
slug = "ai_infra/gguf-llama-cpp"
category = "ai_infra"
tags = ["ai", "gguf", "llama-cpp", "cpu", "quantization"]
description = "GGUF model loader using llama-cpp-python: high performance CPU/GPU offloading"
difficulty = "advanced"
deps = ["llama-cpp-python"]
depends_on = ["ai_infra/model-loader"]

[[files]]
src = "loader.py"
dest = "ai_infra/gguf_loader.py"
merge = "replace"

template_vars = ["GGUF_MODEL_PATH"]
related = ["ai_infra/model-loader"]
