name = "Async Job Webhook"
slug = "ai_infra/async-job-webhook"
category = "ai_infra"
tags = ["ai", "async", "jobs", "webhooks", "background"]
description = "Async inference job: POST enqueue, job_id return, background processing, webhook callback"
difficulty = "advanced"
deps = ["fastapi", "httpx", "uuid"]
depends_on = ["ai_infra/model-loader"]

[[files]]
src = "jobs.py"
dest = "ai_infra/async_jobs.py"
merge = "replace"

[[files]]
src = "router.py"
dest = "routers/jobs.py"
merge = "replace"

template_vars = []
related = ["ai_infra/batch-inference"]
