name = "Single Inference"
slug = "ai_infra/single-inference"
category = "ai_infra"
tags = ["ai", "inference", "endpoint", "pydantic"]
description = "Single-item inference endpoint: input validation, preprocessing, response shaping"
difficulty = "beginner"
deps = ["fastapi", "pydantic"]
depends_on = ["ai_infra/model-loader", "ai_infra/gpu-semaphore"]

[[files]]
src = "inference.py"
dest = "ai_infra/single_inference.py"
merge = "replace"

[[files]]
src = "router.py"
dest = "routers/inference.py"
merge = "replace"

template_vars = []
related = ["ai_infra/batch-inference"]
