name = "GPU Semaphore"
slug = "ai_infra/gpu-semaphore"
category = "ai_infra"
tags = ["ai", "gpu", "concurrency", "performance"]
description = "asyncio.Semaphore to limit concurrent GPU inference requests"
difficulty = "intermediate"
deps = ["fastapi"]
depends_on = []

[[files]]
src = "semaphore.py"
dest = "ai_infra/gpu_semaphore.py"
merge = "replace"

template_vars = ["MAX_CONCURRENT_INFERENCE"]
related = ["ai_infra/batch-inference"]
