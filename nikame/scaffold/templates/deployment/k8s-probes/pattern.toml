name = "K8s Probes"
slug = "deployment/k8s-probes"
category = "deployment"
tags = ["deployment", "k8s", "kubernetes", "probes", "helm"]
description = "Kubernetes manifest snippets: startup, liveness, and readiness probes with optimized timings"
difficulty = "beginner"
deps = []
depends_on = ["observability/health-checks"]

[[files]]
src = "probes.yaml"
dest = "k8s/probes.yaml"
merge = "replace"

template_vars = []
related = ["observability/health-checks"]
