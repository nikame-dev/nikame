name = "Gunicorn Config"
slug = "deployment/gunicorn-config"
category = "deployment"
tags = ["deployment", "gunicorn", "uvicorn", "production", "process-manager"]
description = "Production Gunicorn config: Uvicorn worker, dynamic worker count, timeout settings, logging"
difficulty = "intermediate"
deps = ["gunicorn", "uvicorn"]
depends_on = []

[[files]]
src = "gunicorn_conf.py"
dest = "gunicorn_conf.py"
merge = "replace"

template_vars = []
related = ["deployment/docker-optimized"]
