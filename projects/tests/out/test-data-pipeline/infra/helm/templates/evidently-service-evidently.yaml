apiVersion: v1
kind: Service
metadata:
  name: evidently
  namespace: local
  labels:
    app: evidently
    nikame.module: evidently
spec:
  type: ClusterIP
  selector:
    app: evidently
  ports:
  - port: 8000
    targetPort: 8000
