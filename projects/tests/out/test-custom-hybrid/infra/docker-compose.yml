# Auto-generated by NIKAME — DO NOT EDIT
services:
  postgres:
    image: public.ecr.aws/bitnami/postgresql:16.2.0
    restart: unless-stopped
    environment:
      POSTGRESQL_DATABASE: ${POSTGRES_DB:-app}
      POSTGRESQL_USERNAME: ${POSTGRES_USER:-postgres}
      POSTGRESQL_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRESQL_REPLICATION_MODE: master
      POSTGRESQL_REPLICATION_USER: repl_user
      POSTGRESQL_REPLICATION_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-app}
    volumes:
    - postgres_data:/bitnami/postgresql
    healthcheck:
      test:
      - CMD-SHELL
      - pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-app}
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 60s
    networks:
    - custom-hybrid-db_data
    labels:
      nikame.module: postgres
      nikame.role: primary
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
  postgres-replica:
    image: public.ecr.aws/bitnami/postgresql:16.2.0
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      POSTGRESQL_USERNAME: ${POSTGRES_USER:-postgres}
      POSTGRESQL_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRESQL_MASTER_HOST: postgres
      POSTGRESQL_REPLICATION_MODE: slave
      POSTGRESQL_REPLICATION_USER: repl_user
      POSTGRESQL_REPLICATION_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRESQL_DATABASE: ${POSTGRES_DB:-app}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-app}
    networks:
    - custom-hybrid-db_data
    labels:
      nikame.module: postgres
      nikame.role: replica
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
  pgbouncer:
    image: public.ecr.aws/bitnami/pgbouncer:1.23.1
    restart: unless-stopped
    environment:
      POSTGRESQL_HOST: postgres
      POSTGRESQL_PORT: '5432'
      POSTGRESQL_USERNAME: ${POSTGRES_USER:-postgres}
      POSTGRESQL_PASSWORD: ${POSTGRES_PASSWORD}
      PGBOUNCER_DATABASE: ${POSTGRES_DB:-app}
      PGBOUNCER_POOL_MODE: transaction
      PGBOUNCER_MAX_CLIENT_CONN: '1000'
      PGBOUNCER_DEFAULT_POOL_SIZE: '25'
    depends_on:
      postgres:
        condition: service_healthy
    networks:
    - custom-hybrid-db_backend
    - custom-hybrid-db_data
    labels:
      nikame.module: pgbouncer
      nikame.role: rw
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
  pgbouncer-ro:
    image: public.ecr.aws/bitnami/pgbouncer:1.23.1
    restart: unless-stopped
    environment:
      POSTGRESQL_HOST: postgres-replica
      POSTGRESQL_PORT: '5432'
      POSTGRESQL_USERNAME: ${POSTGRES_USER:-postgres}
      POSTGRESQL_PASSWORD: ${POSTGRES_PASSWORD}
      PGBOUNCER_DATABASE: ${POSTGRES_DB:-app}
      PGBOUNCER_POOL_MODE: transaction
      PGBOUNCER_MAX_CLIENT_CONN: '1000'
      PGBOUNCER_DEFAULT_POOL_SIZE: '25'
    depends_on:
      postgres-replica:
        condition: service_started
    networks:
    - custom-hybrid-db_backend
    - custom-hybrid-db_data
    labels:
      nikame.module: pgbouncer
      nikame.role: ro
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
  postgres-backup:
    image: public.ecr.aws/bitnami/postgresql:16.2.0
    restart: unless-stopped
    environment:
      POSTGRESQL_CLIENT_DATABASE: ${POSTGRES_DB:-app}
      POSTGRESQL_CLIENT_USERNAME: ${POSTGRES_USER:-postgres}
      POSTGRESQL_CLIENT_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRESQL_HOST: postgres
    depends_on:
      postgres:
        condition: service_healthy
    networks:
    - custom-hybrid-db_data
    labels:
      nikame.module: postgres
      nikame.role: backup
    entrypoint:
    - /bin/bash
    - -c
    - while true; do pg_dump -h $POSTGRESQL_HOST -U $POSTGRESQL_CLIENT_USERNAME $POSTGRESQL_CLIENT_DATABASE
      > /backups/backup_$(date +%Y%m%d_%H%M%S).sql; sleep 86400; done
    volumes:
    - postgres_backups:/backups
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
  minio:
    image: minio/minio:latest
    restart: unless-stopped
    command: server /data --console-address ':9001'
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    ports: []
    volumes:
    - minio_data:/data
    healthcheck:
      test:
      - CMD
      - curl
      - -f
      - http://localhost:9000/minio/health/live
      interval: 15s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
    - custom-hybrid-db_frontend
    - custom-hybrid-db_backend
    labels:
      nikame.module: minio
      nikame.category: storage
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
  minio-init:
    image: minio/mc:latest
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: /bin/sh
    command: mc alias set myminio http://minio:9000 $${MINIO_ROOT_USER:-minioadmin}
      $${MINIO_ROOT_PASSWORD} && mc mb --ignore-existing myminio/uploads && mc mb
      --ignore-existing myminio/mlflow
    networks:
    - custom-hybrid-db_backend
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
  evidently:
    image: python:3.11-slim
    restart: unless-stopped
    ports:
    - 8085:8000
    command: bash -c 'pip install evidently && evidently ui --port 8000 --host 0.0.0.0
      --workspace /app/workspace'
    volumes:
    - evidently_data:/app/workspace
    networks:
    - custom-hybrid-db_frontend
    - custom-hybrid-db_backend
    healthcheck:
      test:
      - CMD
      - curl
      - -f
      - http://localhost:8000/
      interval: 30s
      timeout: 10s
      retries: 5
    labels:
      nikame.module: evidently
      nikame.category: ml
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
  prefect:
    image: prefecthq/prefect:2-python3.11
    restart: unless-stopped
    ports:
    - 4200:4200
    environment:
      PREFECT_SERVER_API_HOST: 0.0.0.0
      PREFECT_API_URL: http://localhost:4200/api
      PREFECT_API_DATABASE_CONNECTION_URL: postgresql+asyncpg://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/prefect
    command: prefect server start --host 0.0.0.0
    depends_on:
      postgres:
        condition: service_healthy
    networks:
    - custom-hybrid-db_frontend
    - custom-hybrid-db_backend
    healthcheck:
      test:
      - CMD
      - curl
      - -f
      - http://localhost:4200/api/health
      interval: 30s
      timeout: 10s
      retries: 3
    labels:
      nikame.module: prefect
      nikame.category: ml
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
  mlflow:
    image: ghcr.io/mlflow/mlflow:v2.10.2
    restart: unless-stopped
    ports:
    - 5000:5000
    environment:
      MLFLOW_S3_ENDPOINT_URL: http://minio:9000
      AWS_ACCESS_KEY_ID: ${MINIO_ROOT_USER:-minioadmin}
      AWS_SECRET_ACCESS_KEY: ${MINIO_ROOT_PASSWORD}
      MLFLOW_S3_IGNORE_TLS: 'true'
    command:
    - mlflow
    - server
    - --host
    - 0.0.0.0
    - --port
    - '5000'
    - --backend-store-uri
    - postgresql://user:password@postgres:5432/mlflow
    - --default-artifact-root
    - s3://mlflow
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy
    networks:
    - custom-hybrid-db_frontend
    - custom-hybrid-db_backend
    labels:
      nikame.module: mlflow
      nikame.category: ml
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
  bentoml:
    image: my_bento:latest
    restart: unless-stopped
    ports:
    - 3000:3000
    environment:
      BENTOML_PORT: '3000'
      BENTOML_HOST: 0.0.0.0
    networks:
    - custom-hybrid-db_frontend
    - custom-hybrid-db_backend
    healthcheck:
      test:
      - CMD
      - curl
      - -f
      - http://localhost:3000/livez
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    labels:
      nikame.module: bentoml
      nikame.category: ml
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
  ollama-custom-hybrid-db:
    image: ollama/ollama:latest
    volumes:
    - ollama_data:/root/.ollama
    ports: []
    healthcheck:
      test:
      - CMD
      - curl
      - -f
      - http://localhost:11434/api/tags
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
    - custom-hybrid-db_network
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.12.0
    restart: unless-stopped
    ports: []
    environment:
      discovery.type: single-node
      xpack.security.enabled: 'false'
      ES_JAVA_OPTS: -Xms512m -Xmx512m
    volumes:
    - elasticsearch_data:/usr/share/elasticsearch/data
    networks:
    - custom-hybrid-db_network
    healthcheck:
      test:
      - CMD
      - curl
      - -f
      - http://localhost:9200/_cluster/health
      interval: 30s
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
  mongodb:
    image: mongo:7.0
    restart: unless-stopped
    ports: []
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: ${MONGODB_ROOT_PASSWORD}
    volumes:
    - mongodb_data:/data/db
    networks:
    - custom-hybrid-db_network
    healthcheck:
      test:
      - CMD
      - mongosh
      - --eval
      - db.adminCommand('ping')
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
  api:
    build:
      context: ../app
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
    - 8000:8000
    environment:
      APP_NAME: custom-hybrid-db
      APP_ENV: aws
      UVICORN_WORKERS: '4'
      UVICORN_PORT: '8000'
      CORS_ORIGINS: '*'
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@pgbouncer:5432/${POSTGRES_DB}
      POSTGRES_HOST: pgbouncer
      POSTGRES_PORT: '5432'
      DATABASE_READ_URL: postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@pgbouncer-ro:5432/${POSTGRES_DB}
      POSTGRES_READ_HOST: pgbouncer-ro
      S3_ENDPOINT: http://minio:9000
      S3_ACCESS_KEY: ${MINIO_ROOT_USER:-minioadmin}
      S3_SECRET_KEY: ${MINIO_ROOT_PASSWORD}
      S3_BUCKET: uploads
      EVIDENTLY_HOST: http://evidently:8085
      PREFECT_API_URL: http://prefect:4200/api
      MLFLOW_TRACKING_URI: http://mlflow:5000
      MLFLOW_S3_ENDPOINT_URL: http://minio:9000
      BENTOML_URL: http://bentoml:3000
      ELASTICSEARCH_URL: http://elasticsearch:9200
      MONGODB_URL: mongodb://admin:${MONGODB_ROOT_PASSWORD}@mongodb:27017
      API_URL: http://api:8000
    healthcheck:
      test:
      - CMD
      - curl
      - -f
      - http://localhost:8000/health
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
    - custom-hybrid-db_frontend
    - custom-hybrid-db_backend
    labels:
      nikame.module: fastapi
      nikame.category: api
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
volumes:
  elasticsearch_data: {}
  evidently_data: {}
  minio_data: {}
  mongodb_data: {}
  ollama_data: {}
  postgres_backups: {}
  postgres_data: {}
networks:
  custom-hybrid-db_frontend:
    driver: bridge
  custom-hybrid-db_backend:
    driver: bridge
  custom-hybrid-db_data:
    driver: bridge
    internal: true
  custom-hybrid-db_network:
    driver: bridge
