apiVersion: v1
kind: Service
metadata:
  name: bentoml
  namespace: production
  labels:
    app: bentoml
    nikame.module: bentoml
spec:
  type: ClusterIP
  selector:
    app: bentoml
  ports:
  - port: 3000
    targetPort: 3000
