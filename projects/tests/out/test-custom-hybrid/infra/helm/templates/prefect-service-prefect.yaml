apiVersion: v1
kind: Service
metadata:
  name: prefect
  namespace: production
  labels:
    app: prefect
    nikame.module: prefect
spec:
  type: ClusterIP
  selector:
    app: prefect
  ports:
  - port: 4200
    targetPort: 4200
