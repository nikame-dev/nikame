# Project Guide: rag-platform

This guide provides everything you need to develop, test, and deploy **rag-platform**.

---

## 🚀 Project Overview

rag-platform is a high-performance backend generated by [NIKAME](https://github.com/nikame-framework/nikame). 

### Selected Modules

- **postgres**: PostgreSQL relational database with optional replication and pooling

- **minio**: MinIO S3-compatible object storage

- **prometheus**: Prometheus metrics collection and alerting

- **langchain**: LangChain is the orchestrator for your LLM agents.

- **prefect**: Prefect dataflow and ML pipeline orchestration

- **mlflow**: MLflow experiment tracking and model registry

- **ollama**: Local LLM runner with simple API

- **github_actions**: GitHub Actions — Production-ready CI/CD pipelines

- **otel_collector**: OpenTelemetry Collector for vendor-agnostic telemetry ingestion

- **tempo**: Grafana Tempo high-scale distributed tracing backend

- **loki**: Grafana Loki log aggregation system

- **grafana**: Grafana dashboards with auto-provisioned datasources

- **dragonfly**: Dragonfly — Redis-compatible cache with 25x memory efficiency

- **redpanda**: RedPanda — Kafka-compatible streaming with no JVM, 10x faster

- **qdrant**: Qdrant vector database for AI/ML

- **fastapi**: FastAPI web framework with uvicorn ASGI server


### Active Features


---

## 🛠 Prerequisites

To run this project, you need:
- **Docker & Docker Compose**: (v24.0+)
- **Python**: (v3.11+)
- **Curl**: For testing APIs


---

## ⚙️ Environment Setup

Copy `.env.generated` to `.env` and fill in the required values.

### Important Variables
| Variable | Description | Required | Default |
|----------|-------------|----------|---------|
| `APP_ENV` | Environment (local, staging, prod) | Yes | `local` |


| `POSTGRES_PASSWORD` | Password for the primary database | Yes | - |

























| `REDIS_URL` | Connection string for cache | Yes | `redis://cache:6379` |









---

## 🏃 Starting the Project

1. **Start all services**:
   ```bash
   nikame up
   ```
2. **Verify health**:
   ```bash
   nikame ps
   ```
   All containers should show `Up (healthy)`.

---

## 🔗 Service URLs

| Service | Local URL | Usage | Default Credentials |
|---------|-----------|-------|---------------------|

| PostgreSQL | `localhost:5432` | Primary SQL database | `postgres / ${POSTGRES_PASSWORD}` |

|  | `https://python.langchain.com/` |  | `` |

| API (Swagger) | `http://localhost:8000/docs` | Interactive API documentation | `None` |

| API (Redoc) | `http://localhost:8000/redoc` | Alternative API documentation | `None` |


---

## 📖 API Usage

### Endpoints


---

## ✨ Feature Guides



---

## 🔄 Integrations

How the services connect:

- **FastAPI**: Connects via SQLAlchemy using the `POSTGRES_URL` env var.

- ****: 

- ****: 


---

## 👷 Development Workflow

### Adding a New Endpoint
1. Open `app/main.py`.
2. Add a new route handler using `@app.get` or `@app.post`.

### Database Migrations (Alembic)
If you change your SQLAlchemy models:
```bash
nikame exec api alembic revision --autogenerate -m "description"
nikame exec api alembic upgrade head
```

### Background Jobs (Celery)
If background jobs are enabled, add new tasks in `app/worker.py` and call them with `.delay()`.

---

## 📊 Monitoring

- **Grafana**: Open `http://localhost:3000`. 
- **Dashboards**: Pre-built dashboards for FastAPI, Postgres, and JVM (if applicable).
- **Alerts**: Alerts are configured in Prometheus and sent to Alertmanager.

---

## 🕵️ Troubleshooting

| Issue | Potential Cause | Fix |
|-------|-----------------|-----|

| Database connection refused | Service down or config error | `Run `nikame up` and check if the `postgres` container is healthy.` |

| Authentication failed | Service down or config error | `Ensure `POSTGRES_PASSWORD` in `.env` matches the one used during first start.` |

|  | Service down or config error | `` |

| API not reachable | Service down or config error | `Check if port 8000 is occupied by another process or if the container is running.` |

| Container fails to start | Port conflict | Check `docker ps` for used ports or use `nikame stop` |

---

## 📇 Quick Reference Card

| Action | Command |

| Start the development environment | `nikame up` |

| Stop all services | `nikame stop` |

| Follow logs of all services | `nikame logs -f` |

| Check status of services | `nikame ps` |

| Deploy to local | `nikame deploy` |

---
*Generated by NIKAME on 2026-04-03 17:46:47*