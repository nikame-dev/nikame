apiVersion: v1
kind: Service
metadata:
  name: traefik
  namespace: local
spec:
  type: LoadBalancer
  selector:
    app: traefik
  ports:
  - port: 80
    targetPort: 80
    name: web
  - port: 443
    targetPort: 443
    name: websecure
