apiVersion: v1
kind: Service
metadata:
  name: redpanda
  namespace: local
spec:
  selector:
    app: redpanda
  ports:
  - port: 9092
    targetPort: 9092
    name: kafka
  - port: 8081
    targetPort: 8081
    name: schema-registry
  clusterIP: None
