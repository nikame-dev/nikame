server:
  http_listen_port: 3200

distributor:
  receivers:
    otlp:
      protocols:
        grpc:
        http:

ingester:
  max_block_duration: 5m

compactor:
  compaction:
    compaction_cycle: 1m
    block_retention: 1h

storage:
  trace:
    backend: local
    local:
      path: /var/tempo-data
    wal:
      path: /var/tempo-data/wal