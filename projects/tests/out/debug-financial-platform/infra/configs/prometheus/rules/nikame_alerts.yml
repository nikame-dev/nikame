# Auto-generated by NIKAME — DO NOT EDIT
groups:
- name: nikame-alerts
  rules:
  - alert: PrometheusTargetDown
    expr: up == 0
    for: 5m
    labels:
      severity: warning
    annotations:
      summary: Prometheus target {{ $labels.job }} is down
      description: Target {{ $labels.instance }} has been unreachable for 5 minutes.
  - alert: PrometheusHighMemory
    expr: process_resident_memory_bytes{job='prometheus'} > 4e9
    for: 15m
    labels:
      severity: warning
    annotations:
      summary: Prometheus using >4GB RAM
  - alert: GrafanaDown
    expr: up{job='grafana'} == 0
    for: 1m
    labels:
      severity: critical
    annotations:
      summary: Grafana is down
  - alert: RedPandaDown
    expr: up{job='redpanda'} == 0
    for: 1m
    labels:
      severity: critical
    annotations:
      summary: RedPanda broker is down
  - alert: RedPandaUnderReplicatedPartitions
    expr: redpanda_kafka_under_replicated_replicas > 0
    for: 5m
    labels:
      severity: warning
    annotations:
      summary: RedPanda has under-replicated partitions
  - alert: RedPandaHighLatency
    expr: redpanda_kafka_request_latency_seconds{quantile='0.99'} > 1
    for: 5m
    labels:
      severity: warning
    annotations:
      summary: RedPanda p99 latency above 1 second
  - alert: PostgresDown
    expr: up{job='postgres'} == 0
    for: 1m
    labels:
      severity: critical
    annotations:
      summary: PostgreSQL is down
      description: PostgreSQL has been unreachable for over 1 minute.
  - alert: PostgresHighConnections
    expr: pg_stat_activity_count / pg_settings_max_connections > 0.8
    for: 5m
    labels:
      severity: warning
    annotations:
      summary: PostgreSQL connections above 80%
      description: Connection utilization is critically high.
  - alert: PostgresReplicationLag
    expr: pg_replication_lag > 30
    for: 5m
    labels:
      severity: warning
    annotations:
      summary: PostgreSQL replication lag > 30s
      description: Replication lag has exceeded 30 seconds.
