apiVersion: v1
kind: Service
metadata:
  name: langfuse
  namespace: local
  labels:
    app: langfuse
    nikame.module: langfuse
spec:
  type: ClusterIP
  selector:
    app: langfuse
  ports:
  - port: 3000
    targetPort: 3000
