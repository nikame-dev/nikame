apiVersion: v1
kind: Job
metadata:
  name: postgres-migration
  namespace: production
spec:
  template:
    spec:
      containers:
      - name: migration
        image: test-project-api:latest
        command:
        - alembic
        - upgrade
        - head
        env:
        - name: DATABASE_URL
          value: postgresql+asyncpg://postgres@postgres:5432/test-project
      restartPolicy: OnFailure
  backoffLimit: 4
